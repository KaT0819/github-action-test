name: CI Test Actions
on:
  push:
    branches:
      - 'develop'
      - 'feature/*'

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test
        shell: bash
        run: go test
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
